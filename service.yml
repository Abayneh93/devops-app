apiVersion: v1
kind: Service
metadata:
  name: remote-app-service
spec:
  type: NodePort
  selector:
    app: remote-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
