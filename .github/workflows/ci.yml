name: DevOps App CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Container Image
        run: |
          docker build -t ghcr.io/abayneh93/devops-app:latest .

      - name: Test Container Output
        run: |
          docker run -d --name test-app -p 8000:8000 ghcr.io/abayneh93/devops-app:latest
          sleep 3
          curl http://127.0.0.1:8000

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Image to GHCR
        if: github.event_name != 'pull_request'
        run: |
          docker push ghcr.io/abayneh93/devops-app:latest
